let language = ["JavaScript", "Java", "C++", "C#", "PHP"];


// Push method is used to add the element at the end of the array
//language.push("Python");
//console.log(language);

//pop method is used to remove the last element from the array

/*let myPopLanguage =language.pop();
console.log(myPopLanguage);
console.log(language);*/

//Shift method is used to remove the element at the beginning of the array and displayed the removed element
//let myFirstLanguage = language.shift();
language.shift();
console.log(language);
//console.log(myFirstLanguage);





